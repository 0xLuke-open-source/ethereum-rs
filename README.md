# Ethereum-RS

一个用 Rust 编写的高性能 Ethereum 区块链数据解析与同步服务。

## ✨ 特性

- ✅ 实时同步 Ethereum 主网区块
- ✅ 解析 ETH 转账和 ERC20 代币转账
- ✅ 多 RPC 提供商负载均衡和故障转移
- ✅ 支持 PostgreSQL 数据存储
- ✅ 可配置的同步策略和延迟

## 🚀 快速开始

### 环境要求
- Rust 1.70+ 和 Cargo
- PostgreSQL 13+
- Ethereum RPC 节点（Infura、Alchemy 等）

### 安装与运行
```bash
# 克隆项目
git clone https://github.com/你的用户名/ethereum-rs.git
cd ethereum-rs

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入你的配置

# 安装依赖
cargo build --release

# 运行数据库迁移
cargo run --bin migrate

# 启动区块同步
cargo run --bin sync


src/
├── cli/           # 命令行接口
├── config/        # 配置管理
├── database/      # 数据库连接与迁移
├── errors/        # 全局错误处理
├── infrastructure/# 基础设施（外部服务接入）
├── models/        # 数据模型
├── repositories/  # 数据访问层
├── services/      # 业务逻辑层
├── startup/       # 应用启动逻辑
└── utils/         # 工具类